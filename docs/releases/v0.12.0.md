# Fulvid 0.12.0

Ephemeral Untitled draft recovery with numbered Untitled tabs, plus Session Change Markers and a temporary Before -> Now preview for unsaved session edits.

## Highlights

- **Untitled draft recovery**: unsaved Untitled documents can return after a normal Fulvid restart (ephemeral IndexedDB recovery). Empty or whitespace-only Untitled tabs are not retained. Save As or confirmed discard removes the draft. Not filesystem autosave; clearing WebView/site data can remove recoverable drafts. Real files stay on the filesystem.
- **Untitled numbering**: tabs use `Untitled 1`, `Untitled 2`, ... with the smallest available number among open Untitled tabs. The number is presentation only.
- **Session change markers**: gutter markers show lines changed since the current editor session baseline (modified / added / deleted). Clicking a marker opens a temporary read-only Before -> Now comparison for that change only. Settings -> Editor -> Show session changes (on by default) shows or hides markers and that preview together; turning it off hides presentation only and does not stop baseline tracking. Ephemeral - not Git, not history, not persisted. Save resets the baseline and clears them.

## Download names (when published)

| Platform | Artifact |
| --- | --- |
| Linux x64 (Debian) | `fulvid_0.12.0_linux-x64.deb` |
| Linux x64 (archive) | `fulvid_0.12.0_linux-x64-Setup.tar.gz` |
| macOS Apple Silicon | `fulvid_0.12.0_macos-arm64.dmg` |
| Windows x64 | `fulvid_0.12.0_win-x64-Setup.zip` |

Public GitHub Release may still be gated; see DISTRIBUTION.md.

## Notes

- Draft recovery is Untitled-only. Folder documents remain filesystem-owned.
- Session Change Preview is hunk-scoped and read-only; it never owns the live editor model.
- Disabling Show session changes does not reset the session baseline or change save/undo semantics.
- MDX remains inert source. Export HTML behavior is unchanged.
