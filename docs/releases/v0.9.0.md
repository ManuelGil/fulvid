# Fulvid 0.9.0

Local Lua extensions and quit confirmation for unsaved changes.

## Highlights

- **Extensions (API v1)**: Settings -> Extensions (also File -> Extensions) installs, uninstalls, and reloads local Lua packs under `userData/extensions/`. Packs can contribute commands and, when declared, use notify, editor selection, document read/reveal/untitled, decorations, and bounded `template.render`. Source-only Lua/Wasm on the Bun host; no marketplace, network, or filesystem access. Pack id is `publisher.name` (`local` reserved). Blocked packs can be reviewed and allowed. Fulvid ships no packs in-tree; product packs live in sibling `fulvid-extensions`. Guide: `docs/EXTENSIONS.md`. Author contract: `docs/EXTENSION-AUTHOR-CONTRACT.md`.
- **Quit with unsaved changes**: Quit asks for confirmation when Confirm before closing is on and dirty tabs remain (menu Quit stays a command so the guard cannot be bypassed).

## Download names (when published)

| Platform | Artifact |
| --- | --- |
| Linux x64 (Debian) | `fulvid_0.9.0_linux-x64.deb` |
| Linux x64 (archive) | `fulvid_0.9.0_linux-x64-Setup.tar.gz` |
| macOS Apple Silicon | `fulvid_0.9.0_macos-arm64.dmg` |
| Windows x64 | `fulvid_0.9.0_win-x64-Setup.zip` |

Public GitHub Release may still be gated; see DISTRIBUTION.md.

## Notes

- Extensions are local packs only. There is no marketplace, package manager, or remote install channel.
- Packs do not get network or filesystem access; product behavior beyond the declared capabilities stays in the packs, not the host.
- MDX remains inert source. Preview and Export HTML behavior are unchanged.
