# Fulvid 0.10.0

Explorer folder and document placement, Markdown task toggling, and Cmd/Ctrl-click soft-open for a unique unresolved near-match.

## Highlights

- **New Folder**: create an empty folder in the open Folder from Explorer (toolbar and folder New menu). The parent listing refreshes after creation.
- **Move**: move a document to another folder in the open Folder from the Explorer document menu. Link updates follow the same path as Rename.
- **Toggle task**: flip `[ ]` / `[x]` on Markdown task lines in the editor (Format menu and toolbar). Undoable; non-task lines stay unchanged. Preview stays inert.
- **Cmd/Ctrl-click soft-open**: follow an unresolved document link when it has exactly one near-match, without changing the source text. Zero or several candidates stay non-navigable on that gesture (Go to Definition and Inspector soft-open unchanged).

## Download names (when published)

| Platform | Artifact |
| --- | --- |
| Linux x64 (Debian) | `fulvid_0.10.0_linux-x64.deb` |
| Linux x64 (archive) | `fulvid_0.10.0_linux-x64-Setup.tar.gz` |
| macOS Apple Silicon | `fulvid_0.10.0_macos-arm64.dmg` |
| Windows x64 | `fulvid_0.10.0_win-x64-Setup.zip` |

Public GitHub Release may still be gated; see DISTRIBUTION.md.

## Notes

- New Folder creates one empty directory; it is not recursive folder management.
- Move reuses rename semantics (no silent overwrite; link rewrite as for Rename).
- Soft-open navigates only; it does not rewrite the source link.
- Preview remains inert for task checkboxes.
- MDX remains inert source. Export HTML behavior is unchanged.
