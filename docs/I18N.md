# UI strings and translations

Visible chrome lives in [`src/mainview/i18n/`](../src/mainview/i18n/) (`en`, `es`, `de`, `fr`, `it`, `nl`, `pt`). Wiring: [`src/mainview/i18n/index.ts`](../src/mainview/i18n/index.ts). Vocabulary: [CONCEPTS.md](./CONCEPTS.md).

`en` is the semantic source. Every other locale expresses the same product meaning in natural local UI language. Do not translate word for word. Do not chain translations through Spanish.

Write for the person using Fulvid. Never drop a technical contract (placeholder, escape, example syntax) to make a sentence prettier.

Prefer: correct meaning, correct UI context, natural phrasing, then brevity. If a string is already natural and accurate, leave it.

## Supported locales

| Code | Language name in Settings (native endonym) |
| --- | --- |
| `en` | English |
| `es` | Español |
| `de` | Deutsch |
| `fr` | Français |
| `it` | Italiano |
| `nl` | Nederlands |
| `pt` | Português |

Language names in the selector stay as native endonyms in every locale.

`pt` uses neutral Portuguese suitable for a global desktop product (not a strongly regional Brazilian or European variant).

## Context

Read the control, the label next to it, and the action it performs. The same idea may need a different shape as a button, tooltip, hint, empty state, error, menu, or statusbar item.

- A Context button that opens Graph is **Show in Graph** / **Ver en el Grafo**, not a fragment like "See where".
- Search finds text in documents. Do not describe it as "Find a document". Quick Open finds a document by name or path; do not describe it as content search.
- Ctrl+Shift+Enter toggles **Writing Focus**. That is not Graph Focus. Ctrl+Shift+F opens **Global Search**. Ctrl+P is reserved for **Quick Open**. F11 (Ctrl+Cmd+F on macOS) toggles native Full Screen.
- **Focus** (Graph / Document Context) is not **Writing Focus**. Keep that distinction in every locale.

Hints and tooltips must add information the label does not already give. Settings hints should say what changes, when it applies, what the options mean, and a real example when that helps. Do not start with "This setting controls..." or repeat the label.

Empty states should name the empty thing once. Under **Incoming links**, prefer **No incoming links yet.**

Errors explain what happened, stay calm, and include a next step when one exists. Do not invent causes or leak host paths, errno, or stack traces.

## Syntax that must stay exact

Rewrite the surrounding sentence. Keep these exactly as Fulvid uses them:

```text
[[note]]
[[note|Label]]
[label](note.md)
[label](note.md#heading)
[label](#heading)
#heading
{date}
{count}
{name}
{path}
{from}
{to}
{files}
{target}
{anchor}
Ctrl+N
Ctrl+P
Ctrl+Shift+F
Ctrl+Shift+Enter
Shift+F12
F2
.md
.markdown
.mdx
.html
```

Do not translate those examples. Keep `[[note]]` and the English path in `[label](note.md)`.

In TypeScript catalog strings, keep real newlines as `\n`. The UI must not show a literal `\n`.

## The `|` rule (vue-i18n)

vue-i18n treats `|` as a plural separator. An unescaped pipe truncates the message.

To show a wikilink alias, the TypeScript source must use `\\|` so the catalog value still contains `\|`:

```ts
wikilinksHint:
  "Treat [[note]] and [[note\\|Label]] as document links.";
```

```text
TypeScript source:   [[note\\|Label]]
Catalog string:      [[note\|Label]]
Visible UI:          [[note|Label]]
```

`bun run i18n:check` fails on an unescaped `|` in a catalog value. Do not use `|` as a visual separator in catalog messages. Prefer commas or ASCII hyphens in compact summaries instead.

If the i18n stack changes, re-check this escape against vue-i18n before editing the rule.

## Placeholders

`{count}`, `{name}`, `{path}`, and the other `{...}` tokens are a contract across locales. Do not rename, translate, or drop them. Word order around a placeholder may change with the language.

`i18n:check` compares placeholder names and counts between catalogs.

## Punctuation

Do not add typographic Unicode just to look editorial. Prefer ASCII `-`, `"`, `'`, `...`, and `>` for menu paths (`View > Writing Focus`).

Locale letters with diacritics stay in that locale's catalog (`á`, `ñ`, `ä`, `ß`, `é`, `ç`, `ã`, `¿`, `¡`, and similar). Do not strip legitimate language characters.

Avoid unless there is a consistent, existing reason in the same UI:

```text
en-dash em-dash curly-quotes ellipsis arrows guillemets
```

Also avoid non-breaking spaces, zero-width characters, and decorative symbols in catalog strings.

`tests/scripts/humanLanguageAscii.unit.test.ts` fails the suite when ambiguous Unicode symbols reappear in repository-controlled `src/`, `tests/`, `docs/`, `scripts/`, packaging, workflows, or root product docs. Latin letters with diacritics are allowed in `src/mainview/i18n/*.ts` and this file.

## Terminology

Product UI says **folder**, never **workspace**. Code may still use `workspace*` names; see [CONCEPTS.md](./CONCEPTS.md).

Do not expose internals: `buffer`, `store`, `RPC`, `provider`, `resolver`, `renderer`, `Monaco` (except About).

| English | es | de | fr | it | nl | pt |
| --- | --- | --- | --- | --- | --- | --- |
| document | documento | Dokument | document | documento | document | documento |
| file | archivo | Datei | fichier | file | bestand | ficheiro |
| folder | carpeta | Ordner | dossier | cartella | map | pasta |
| link | enlace | Link | lien | collegamento | koppeling | ligação |
| heading | encabezado | Überschrift | titre | intestazione | kop | cabeçalho |
| preview | vista previa | Vorschau | aperçu | anteprima | voorbeeld | pré-visualização |
| Writing Focus | Foco de escritura | Schreibfokus | Mode écriture | Modalità scrittura | Schrijffocus | Foco de escrita |
| Focus (Graph/Context) | Foco | Fokus | Focus | Focus | Focus | Foco |
| Context | Contexto | Kontext | Contexte | Contesto | Context | Contexto |
| Graph | Grafo | Graph | Graphe | Grafo | Graf | Grafo |
| Explorer | Explorador | Explorer | Explorateur | Esploratore | Verkenner | Explorador |
| Settings | Ajustes | Einstellungen | Paramètres | Impostazioni | Instellingen | Definições |
| Untitled | Sin título | Unbenannt | Sans titre | Senza titolo | Naamloos | Sem título |
| Save as | Guardar como | Speichern unter | Enregistrer sous | Salva con nome | Opslaan als | Guardar como |

Match capitalization to the same kind of control nearby. Native OS menu roles may stay Title Case (`Select All`, `Hide Others`). German nouns keep their capital letters. Do not copy English title case into other languages.

English: short labels; extra detail in hints. Other locales: natural contemporary software wording for that language, not a calque and not bureaucratic.

## Boundaries (intentionally not in host catalogs)

- Extension pack `displayName`, command titles, and Lua `ui.notify` text stay pack-owned.
- README / document starters and Monaco snippet tab-stop placeholders are document content, not UI chrome.
- Decorative theme-preview mock labels (`aria-hidden`) may stay English.

## Adding a locale

1. Add `src/mainview/i18n/<language>.ts` with the same key tree as `en.ts`.
2. Register it in `src/mainview/i18n/index.ts` `messages`.
3. Add the locale to `Locale`, `VALID_LOCALES`, and the Settings language select in `SettingsPage.vue`.
4. Add the language endonym keys under `settings.*` in every catalog.
5. Run `bun run i18n:check`.

The auditor treats the first locale in sorted order as the structural reference (`de` today).

## Validation

After changing catalogs or `t(...)` usage, run `bun run i18n:check`. The contributor gate is `bun run validate` ([CONTRIBUTING.md](../CONTRIBUTING.md)).

`i18n:check` blocks on missing/extra keys, type mismatches, placeholder mismatches, unescaped `|`, missing static `t()` keys, and hardcoded `aria-label` / `placeholder` / similar attributes. Dynamic keys are printed for review and do not fail the gate.

Check the changed strings in the running app when you can (Settings hints wrap; Graph and Context empty states sit under headings).
