# Examples

`demo-workspace/` is a small public folder for local checks and product screenshots. It is not an installer.

- `getting-started.md` - Markdown with links
- `components.mdx` - MDX source that Fulvid does not execute

Open it with **Open folder...**. Use a copy if you need paths that are not a personal home directory.
