/**
 * Electrobun 2 build orchestration.
 *
 * Keep Bun as the package manager so Bun's lockfile remains authoritative for
 * the project dependencies.
 */
export default {
  packageManager: "bun",
};
